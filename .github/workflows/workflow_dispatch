name: Manual Dispatch Pipeline

on:
  workflow_dispatch:     # this creates manual trigger button

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Manual pipeline triggered successfully 🚀"

      - name: Show date
        run: date

      - name: Show files
        run: ls -al
